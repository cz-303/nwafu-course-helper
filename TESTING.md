# 浏览器验收

## 已完成的离线验证（2026-09-19）

Node.js 语法检查通过。使用无头 Edge 和本地模拟页面，拦截全部网络请求后验证以下 8 项，全部通过：

- 已满时不点击；手动停止清除所有定时器。
- 有余量时只点击一次，点击前撤销自动恢复。
- 仅提醒模式不点击。
- 相同课号多个教学班暂停。
- 未知容量格式暂停。
- 初始页面显示登录失效时清除恢复状态。
- 持久化状态恢复后，页面出现登录失效提示时停止。
- 课程表异步出现后能够检查并触发一次点击。

这些测试没有连接学校服务器、没有登录或提交选课，不能代替真实页面验收。

## 真实页面待验收

下面是需要在实际系统完成的检查，不代表已经通过。先禁用自动点击进行验证。

1. 手动登录，在课程列表看到面板；四个默认课程号正确。
2. 开始后约 60 秒刷新；刷新后恢复监测，课程号和间隔保持。
3. 点击停止，等待超过配置间隔，页面不再自动刷新。
4. 刷新后课程表延迟出现时，脚本等待而不是立即报告无余量。
5. 当前页没有某个目标课号时提示缺失，不报告“四门全部已满”。
6. 用本人确实要选且有余量的课程测试仅提醒：显示容量并暂停，无点击。
7. 开启自动点击后测试：只点击该课程操作列的按钮一次，并暂停；不会点击其他弹窗确认。
8. 点击后即使失败、出现弹窗或刷新，也不会自动重复提交。
9. 登录自然失效时停止刷新；手动登录后需重新开始。
10. 相同课号出现多个教学班时暂停；未知容量格式时暂停。
11. 筛选、分页经刷新被重置时，面板应能反映当前实际可见课程。
12. 每轮运行两小时后停止，需手动重新开始。

选课是否成功只能通过系统返回及“已选课程”确认。
